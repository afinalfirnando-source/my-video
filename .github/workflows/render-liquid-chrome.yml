name: Render LiquidChrome

on:
  push:
    branches: [main, master]
    paths:
      - "src/templates/liquid-chrome/**"
      - "remotion.config.ts"
      - ".github/workflows/render-liquid-chrome.yml"
  workflow_dispatch:

jobs:
  render-liquid-chrome:
    name: Render LiquidChrome (100-250MB)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Bundle
        run: npx remotion bundle

      - name: Render LiquidChrome
        run: |
          npx remotion render \
            "LiquidChrome" \
            "out/liquid-chrome.mp4" \
            --codec=h264 \
            --bitrate=80M \
            --preset=fast \
            --width=3840 \
            --height=2160 \
            --fps=60

      - name: Check file size
        run: |
          FILE_SIZE_MB=$(du -m "out/liquid-chrome.mp4" | cut -f1)
          echo "File size: ${FILE_SIZE_MB} MB"
          if [ "$FILE_SIZE_MB" -lt 100 ]; then
            echo "ERROR: File size ${FILE_SIZE_MB} MB is below 100 MB target"
            exit 1
          elif [ "$FILE_SIZE_MB" -gt 250 ]; then
            echo "ERROR: File size ${FILE_SIZE_MB} MB is above 250 MB target"
            exit 1
          else
            echo "OK: File size ${FILE_SIZE_MB} MB is within 100-250 MB range"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: "liquid-chrome-video"
          path: "out/liquid-chrome.mp4"
          retention-days: 14
